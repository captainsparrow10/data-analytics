# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pyright/
.ruff_cache/
.mypy_cache/

# macOS
.DS_Store

# Books / reference PDFs (no versionar binarios pesados)
*.pdf

# Chapter 13 downloads the book's real datasets on first run into this cache;
# never commit the downloaded data (some files are very large, e.g. the FEC CSV).
cap_13_examples/datasets/
